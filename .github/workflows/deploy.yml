# GitHub Pages deploy — DESACTIVADO
# El deploy ahora corre en Vercel (proyecto jaralab-os).
# Este archivo se conserva para historial. Para reactivar, cambiar 'on: {}' por el trigger original.

name: Deploy Dashboard to GitHub Pages (desactivado)

on: {}

permissions:
  contents: read

jobs: {}
